@{
    Layout = "~/Administration/Views/Shared/_AdminPopupLayout.cshtml";
}
@model SubscriptionOrderModel
@{    
    //page title
    ViewBag.Title = T("Admin.SubscriptionOrders.Fields.PartialRefund").Text;
}
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="section-header">
        <div class="title">
            <img src="@Url.Content("~/Administration/Content/images/ico-sales.png")" alt="" />
            @T("Admin.SubscriptionOrders.Fields.PartialRefund")
        </div>
        <div class="options">
        </div>
    </div>
    
    @Html.ValidationSummary(false)
    
    if (ViewBag.RefreshPage == true)
    {
    <script type="text/javascript">
        try {window.opener.document.forms['@(ViewBag.formId)'].@(ViewBag.btnId).click();}
        catch (e){} 
        window.close();
    </script>
    }
    <p>
        <strong>
            @string.Format(T("Admin.SubscriptionOrders.Fields.PartialRefund.SubscriptionInfo").Text, Model.Id)</strong>
    </p>
    <table width="100%">
        <tr>
            <td class="adminTitle">
                @Html.NopLabelFor(model => model.AmountToRefund):
            </td>
            <td class="adminData">
                @Html.EditorFor(model => model.AmountToRefund): &nbsp;&nbsp;&nbsp; <span style="white-space: nowrap">
                    @string.Format(T("Admin.SubscriptionOrders.Fields.PartialRefund.AmountToRefund.Max").Text, Model.MaxAmountToRefund.ToString("G29"), Model.PrimaryStoreCurrencyCode)</span>
            </td>
        </tr>
    </table>
    <input type="submit" name="partialrefundorder" value="@T("Admin.SubscriptionOrders.Fields.Refund")" id="partialrefundorder" class="adminButton" />
}